# 10 RAII（资源获取即初始化）

## 讲解

RAII 是 C++ 资源管理核心模式：
- 构造函数获取资源
- 析构函数释放资源

Java 对照：
- 类似 `try-with-resources`，但 C++ 更通用，因为对象离开作用域就自动析构。

价值：
- 正常返回、异常抛出、提前 `return` 都能自动清理资源。

## 示例代码

见：`examples/10_raii.cpp`
